kind: Deployment
apiVersion: apps/v1
metadata:
    labels:
        app: randomgenerator-microservice
        stack: frontend
        source: demo
    name: randomgenerator-microservice
    namespace: microservices
spec:
    replicas: 3
    selector:
        matchLabels:
            run: randomgenerator-microservice
    template:
        metadata:
            labels:
                app: randomgenerator-microservice
                run: randomgenerator-microservice
                source: demo
        spec:
            containers:
                -   image: 702037529261.dkr.ecr.us-west-2.amazonaws.com/randomgenerator-microservice
                    name: randomgenerator-microservice
                    ports:
                        -   name: web
                            containerPort: 8080
---

kind: Service
apiVersion: v1
metadata:
    labels:
        app: randomgenerator-microservice-service
    name: randomgenerator-microservice-service
    namespace: microservices
spec:
    selector:
        source: demo
        app: randomgenerator-microservice
    ports:
        -   name: web
            protocol: TCP
            port: 30201
            targetPort: 8080

---

apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
    annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /$2
    name: randomgenerator-ingress
    namespace: microservices
spec:
    rules:
        -   http:
                paths:
                    -   path: /randomgenerator(/|$)(.*)
                        backend:
                            serviceName: randomgenerator-microservice-service
                            servicePort: 30201
